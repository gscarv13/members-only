<h1>Create a new post</h1>

<%= form_with model: @post do |f| %>
	<%= f.label(:title, "Title: ") %>
	<%= f.text_field (:title) %><br><br>

	<%= f.label(:body, "Post: ") %>
	<%= f.text_area (:body) %><br><br>

	<%= f.submit %>
<% end %>